Fixpack L
- demo_seed: fallback to admin key if vehicle/cloud/edge keys are missing in keyring.
  Also best-effort register edges before seeding.
- keeps Fixpack K changes: timezone import + query retry without --node for newer tbthreed.
